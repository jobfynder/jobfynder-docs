# Parking Lot

Ideas and tasks that are real but not now. Review during Weekly Review; promote or drop deliberately — don't let items sit here forever unreviewed.

## Format

**Item** — why it's parked — what would trigger picking it up

## Current items

- **Hermes durable knowledge layer (OKF)** — deferred until a second Hermes agent ships — pick up when that happens.
- **Hermes live-data MCP** — deferred until Phase 1 agents are actually running — pick up then.
- **A documentation-serving Dev MCP for `jobfynder-docs`** — deferred until the knowledge base is too large to paste directly into a prompt — pick up when that becomes true, not before.
- **An Agent MCP for Hermes to operate the platform** — deferred until Phase 1 agents are running, same trigger as the live-data MCP.
- **Marketing content automation via Dittofeed** — deferred until an agent has drafted real marketing content by hand a few times and the output is trusted — pick up once that track record exists.
- **`jobfynder-admin/jobfynder-resume-builder` repo** — real unmerged work on `development` and `added-new-linkedIn-apis` branches (LinkedIn OAuth flow, Postgres→MongoDB migration), 11+ months stale — needs a decision: merge, restart, or formally park.
- **`SUMMARY.md` volumes with no real content (Contracts, DevOps, Security as separate volumes)** — folded into existing docs rather than left as empty promises; see `SUMMARY.md` for where each now lives.
- **"ERS" (Volume 7 in the old `SUMMARY.md`)** — no known meaning anywhere else in this repo. Removed from the table of contents rather than left empty. If it meant something specific, it needs to come back with real content, not as another placeholder.
