# Recover Docker

**No real content exists anywhere in this repo to fill this from.** Template only.

Known context: the platform runs on Docker Compose at Stage 1 (`ADR-0004`), with containers including `jobfynder-comm-gateway` (COMM-1) and Hermes gateway services (admin server). No documented recovery procedure exists for a Docker daemon or container-level failure.
