# Recover DigitalOcean

## Confirmed

Two DigitalOcean servers exist at Stage 1 (`ADR-0004`): COMM-1 (`152.42.219.165`) and, very likely, the Hermes admin/sourcing gateway server (`167.71.217.230`) — see `Infrastructure/SERVER-INVENTORY.md` for why that second mapping is probable but not fully confirmed.

## Unknown — needs someone with live access to fill in

Actual rebuild steps, whether droplet snapshots are taken, and recovery time expectations.
